# 必填：用于管理员 JWT 签名（建议使用 >= 32 位随机字符串）
JWT_SECRET=

# 必填：仅用于“首次初始化管理员账号”（数据库里还没有任何 Admin 时生效）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me

# ChatGPT Team：请在管理后台添加 Team，并提供 Cookies（包含 __Secure-next-auth.session-token）

# 数据库：
# - 本地开发默认使用 `prisma/dev.db`（无需配置）
# - 生产环境建议使用 Turso/libsql，并配置以下变量
DATABASE_URL=
DATABASE_AUTH_TOKEN=

# 可选：日志级别（debug/info/warn/error）
LOG_LEVEL=debug
